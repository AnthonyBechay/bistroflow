services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bistroflow
      POSTGRES_PASSWORD: bistroflow_secret
      POSTGRES_DB: bistroflow
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bistroflow"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: always
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=postgresql://bistroflow:bistroflow_secret@postgres:5432/bistroflow
      - JWT_SECRET=bistroflow-jwt-secret-change-me
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - PORT=3001
      - NODE_ENV=production
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
